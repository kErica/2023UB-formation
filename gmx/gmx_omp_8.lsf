#!/bin/bash

# BSUB -J gmx_omp_8
# BSUB -eo gmx_omp_8.log
# BSUB -n 8
# BSUB -q cursnow
# BSUB -R prades
# BSUB -R span[hosts]=1
  
. /opt/modules/default/init/bash
module load gromacs/2016-3
source /prod/GROMACS/2016-3/bin/GMXRC
OMP_NUM_THREADS=8

lsf_copy $PWD/ion_channel.tpr
cd $TMPDIR
gmx_mpi mdrun -ntomp $OMP_NUM_THREADS -ntomp_pme 0 -s ion_channel.tpr
